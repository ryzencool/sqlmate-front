import create from "zustand";
import {atom} from "jotai";

export const tableListAtom = atom([])


export const activeTableAtom  = atom(0)

export const isOnProjectAtom = atom(true)


export const tableListDetailAtom = atom([])

export const tableRelsAtom = atom([])
